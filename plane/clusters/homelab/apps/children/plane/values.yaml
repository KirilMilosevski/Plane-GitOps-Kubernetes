planeVersion: "v1.4.1"

ingress:
  enabled: true
  ingressClass: "traefik"
  ingressClassName: "traefik"
  appHost: "plane.homelab"
  minioHost: "plane-minio.homelab"


postgres:
  storageClass: "local-path"

redis:
  storageClass: "local-path"

minio:
  storageClass: "local-path"
  
