planeVersion: "v1.4.1"

ingress:
  enabled: true
  ingressClass: "traefik"
  appHost: "plane.homelab"
  minioHost: "plane-mini-homelab"


postgres:
  storageClass: "local-path"

redis:
  storageClass: "local-path"

minio:
  storageClass: "local-path"
  
